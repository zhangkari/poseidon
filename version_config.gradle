ext.depends = [:]

def artifact = [:]
artifact.VName = "0.0.4"
artifact.VCode = 000004

def build = [:]
build.compileSdkVersion = 28
build.minSdkVersion = 21
build.targetSdkVersion = 28

def versions = [:]
versions.support = "28.0.0"

def support = [:]
support.appcompat = "androidx.appcompat:appcompat:1.2.0"
support.constraitLayout = 'androidx.constraintlayout:constraintlayout:2.0.4'

def libs = [:]
libs.eventBus = "org.greenrobot:eventbus:3.1.1"
libs.json = 'com.google.code.gson:gson:2.8.5'

def test = [:]
test.junit = "junit:junit:4.12"
test.andJunit = "androidx.test.ext:junit:1.1.2"
test.espresso = 'androidx.test.espresso:espresso-core:3.3.0'

def depends = [:]
depends.artifact = artifact
depends.build = build
depends.libs = libs
depends.support = support
depends.test = test
ext.depends = depends